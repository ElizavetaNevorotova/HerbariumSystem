<p id="notice"><%= notice %></p>

<h1>Taxons</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Level</th>
      <th>Parent</th>
      <th>Exemplar</th>
      <th>Botanist</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @taxons.each do |taxon| %>
      <tr>
        <td><%= taxon.name %></td>
        <td><%= taxon.level %></td>
        <td><%= taxon.parent_id %></td>
        <td><%= taxon.exemplar_id %></td>
        <td><%= taxon.botanist_id %></td>
        <td><%= link_to 'Show', taxon %></td>
        <td><%= link_to 'Edit', edit_taxon_path(taxon) %></td>
        <td><%= link_to 'Destroy', taxon, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Taxon', new_taxon_path %>
