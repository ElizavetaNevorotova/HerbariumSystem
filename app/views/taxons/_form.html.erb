<%= form_with(model: taxon, local: true) do |form| %>
  <% if taxon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taxon.errors.count, "error") %> prohibited this taxon from being saved:</h2>

      <ul>
      <% taxon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="field">
    <%= form.label :parent_id %>
    <%= form.number_field :parent_id %>
  </div>

  <div class="field">
    <%= form.label :exemplar_id %>
    <%= form.number_field :exemplar_id %>
  </div>

  <div class="field">
    <%= form.label :botanist_id %>
    <%= form.number_field :botanist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
